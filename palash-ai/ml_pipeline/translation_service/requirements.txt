# ────────────────────────────────────────────────────────────────────────────
# PALASH-AI — Python Translation Service dependencies
#
# Install inside a virtual environment:
#   python -m venv .venv
#   # Windows:  .venv\Scripts\activate
#   # Linux/Mac: source .venv/bin/activate
#   pip install -r requirements.txt
#
# First startup downloads ai4bharat/indictrans2-indic-indic-dist-320M
# (~1.3 GB) into the Hugging Face cache (~/.cache/huggingface).
# Subsequent startups reuse the cached files.
# ────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.14
uvicorn[standard]==0.34.3

# IndicTrans2 inference toolkit (IndicProcessor pre/post-processing)
# Requires numpy>=2.1, torch>=2.5, transformers>=4.37
indictranstoolkit==1.1.1

# Deep learning & tokenisation
# IMPORTANT: transformers must stay at 4.37.x
# transformers>=4.38 changed past_key_values to DynamicCache which is
# incompatible with the IndicTrans2 custom modeling code.
torch>=2.5.0
transformers==4.37.2
sentencepiece>=0.2.0
sacremoses>=0.1.1

# Numerical
numpy>=2.1.0

# HTTP validation (already a FastAPI transitive dep, pinned explicitly)
pydantic>=2.0.0
